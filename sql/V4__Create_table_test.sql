create table temp(
  id integer, -- auto incrementing IDs   
  name varchar(100),  -- variable string column
  preferences varchar(100), -- column used to store JSON type of data
  created_at timestamp
);